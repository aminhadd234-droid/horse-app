/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // You can add i18n here if needed later
  // i18n: { locales: ['en','ar'], defaultLocale: 'en' }
}
module.exports = nextConfig
